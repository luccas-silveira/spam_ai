version: "3.9"

services:
  ghl-webhooks:
    build:
      context: .
    image: ghl-base:latest
    env_file:
      - .env
    ports:
      - "8081:8081"
    volumes:
      - ghl_data:/app/data
    restart: unless-stopped
    command: ["ghl-webhooks"]

volumes:
  ghl_data:
