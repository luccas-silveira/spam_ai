# ===========================================
# Configuração do Servidor de Webhooks
# ===========================================

# Porta do servidor webhook
PORT=8080

# Módulos de handlers a serem carregados (separados por vírgula)
# Aceita sufixo .* para auto-carregar submódulos
WEBHOOK_HANDLERS=handlers.webhooks

# ===========================================
# Credenciais GoHighLevel / LeadConnector
# ===========================================

# Client ID do seu app no Marketplace GHL
# IMPORTANTE: Nunca commitar este arquivo com valores reais!
CLIENT_ID=your-client-id-here

# Client Secret do seu app no Marketplace GHL
CLIENT_SECRET=your-client-secret-here

# PIT (Platform Integration Token) se aplicável
PIT=your-pit-token-here

# ===========================================
# Segurança de Webhooks
# ===========================================

# Segredo compartilhado para validação HMAC de webhooks (opcional)
# WEBHOOK_SECRET=your-webhook-secret-here

# Cabeçalho onde a assinatura HMAC é enviada
# WEBHOOK_SIGNATURE_HEADER=X-Signature

# Algoritmo de hash para validação (sha256 ou sha1)
# WEBHOOK_SIGNATURE_ALGO=sha256

# ===========================================
# Idempotência (cache in-memory)
# ===========================================

# Habilitar idempotência para evitar processamento duplicado
IDEMPOTENCY_ENABLED=true

# TTL do cache de idempotência em segundos
IDEMPOTENCY_TTL_SECONDS=600

# Cabeçalhos HTTP a verificar para chave de idempotência
IDEMPOTENCY_HEADERS=Idempotency-Key,X-Event-Id

# ===========================================
# Gemini API (Google AI) para Detecção de Spam
# ===========================================

# API key do Google Gemini AI
# Obtenha sua chave gratuita em: https://aistudio.google.com/app/apikey
# Tier gratuito: 15 requisições/minuto, 1500 requisições/dia
# Modelo usado: gemini-1.5-flash (rápido e econômico)
GEMINI_API_KEY=your-gemini-api-key-here

# ===========================================
# Rotas
# ===========================================

# Caminho para arquivo de configuração de rotas (opcional)
# WEBHOOK_ROUTES_CONFIG=config/routes.json
